@model IEnumerable<TheBoxApp_Web.Aditional>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TX_Aditional)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BO_Active)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DT_Register)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TX_Aditional)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BO_Active)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DT_Register)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Aditional }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Aditional }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Aditional })
        </td>
    </tr>
}

</table>
